#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main() {

	ll a, b, c, d;
	vector<int>v;
	cin >> a >> b >> c >> d;

	v.push_back(b);
	v.push_back(c);
	v.push_back(d);

	sort(v.begin(), v.end(), greater<int>());

	for (auto q : v) {

	}





}