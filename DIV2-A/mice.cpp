#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main() {

	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	ll t, q;
	cin >> t;

	while (t--) {
		map<int, int>m;
		vector<int>diff;
		cin >> n >> k;

		while (k--) {
			cin >> q;
			++m[q];
			diff.push_back(n - q);
		}
		sort(v.begin(), v.end(), greater<int>());

		ll cat = 0;



	}
	return 0;
}

